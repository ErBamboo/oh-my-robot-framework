# Flash / Bootloader / OTA 设计参考档案（锚点文档）

> **状态**：工作锚点——持续回读、校验、更新。本文档是 flash 驱动 + bootloader + OTA 方向的**调研与原则档案**，不是 ADR；决策落地为 ADR 时以 `P-xx` / `K-xx` 编号引用本文，不复述。
>
> **约定**：`P-xx` = 用户拍板的分层定界原则（不可走回头路）；`K-xx` = 从成熟开源框架提炼的考量（随调研更新）。新增条目只在文末"更新记录"追加并编号，**不重排历史编号**（防误伤引用）。原则性内容修订需在更新记录说明动机。
>
> **首版日期**：2026-09-06（素材：2026-09-05 方向定调会话 + 09-06 成熟框架调研）

---

## 1. 方向与开工序列（2026-09-05 定调）

近期目标：**IAP（在应用编程）bootloader + OTA 子系统**——用户区自写引导 + 在线升级（2026-09-06 纠名：方向词为 IAP；原记录 IAR 系误记，相应工具链条目删除）。开工序列（Flash 抽象为第一项）：

1. **Flash 设备抽象层**（片内 FlashDev：read/write/erase + geometry；drivers 层 + 平台适配器 + device_find 注册）——地基，零上层依赖
2. **Bootloader 最小工程**（IAP bootloader——基于 FlashDev，平台无关；片内双区布局；bootloader 日志 = 裁剪契约/早期滞留/零 OSAL 轮询三件套）
3. **OTA 状态机**（app 侧升级服务：分区/版本/校验/元数据小块——结构化独立存储，≠日志流）
4. **外部 Flash 批次**（W25Q 设备 + 磨损环形）+ **持久化日志后端**（同批共建；OTA 排障现场记录 = 头号消费场景）

## 2. 分层定界原则（P-xx——用户拍板，本方向架构的不可退让条款）

| 编号 | 原则 | 拍板日期 | 校验点（动代码前自问） |
|---|---|---|---|
| P-01 | Flash 驱动 = 设备服务抽象（PAL/设备模型族）；**片内 Flash 同样需要**；上层（bootloader/OTA/持久化）**严禁直接调用第三方库**，厂商驱动只出现在平台适配器内 | 2026-09-05 | 上层代码里出现 HAL_FLASH / driverlib 符号了吗？ |
| P-02 | 换芯片 = 换适配器：同一上层抽象，STM32F4 与 MSPM0 只差平台适配实现，上层零改动 | 2026-09-05 | 适配器新增/修改时上层文件动了吗？ |
| P-03 | 分区表抽象（boot/appA/appB/meta 布局）是**驱动上层语义**，与 FlashDev 同批或紧随，但不混入 FlashDev 设备层 | 2026-09-05 | 设备层头文件里出现"分区/boot/app"概念了吗？ |
| P-04 | 上层只表达"槽位 + 指针"；**remap / 介质映射 / bank 切换等细节 = 每板 bootloader 适配职责**（参考：MCUboot direct 模式、Zephyr 板级链接布局） | 2026-09-06 | 槽位语义代码里出现寄存器/remap/介质地址了吗？ |
| P-05 | **磨损环形 = 存储/日志层职责**（SFUD 之上、FAL 之下的边界同构）；不下沉进 FlashDev 设备层，不上浮到 bootloader | 2026-09-06 | FlashDev / bootloader 里出现环形/磨损/擦写均衡了吗？ |

## 3. 参考系统全景

| 参考 | 地位 | 主要借鉴面 |
|---|---|---|
| CMSIS-Driver Flash（ARM） | 事实标准设备 API | FlashDev 接口形状：read/write(program)/erase + GetInfo |
| Zephyr flash API + flash_map | 设备几何与逻辑分区分离范本 | 对齐/擦除语义参数化；分区 = 名字+设备+偏移+大小 |
| MCUboot（Zephyr 生态） | 升级策略集大成者 | swap/overwrite/direct 取舍；trailer 状态机；确认/回滚语义 |
| ESP-IDF OTA + otadata | A/B 双槽工程范本 | 元数据小块（双份轮转 + seq + CRC）；启动计数回滚 |
| RT-Thread FAL / SFUD / QBoot | 同竞赛生态，文件级参考 | FAL 分区表；SFUD NOR 驱动形态；下载→校验→搬移流程 |
| OpenBLT（Feaser） | 单区签名 + 后门 bootloader | 镜像内签名/后门标志；防砖通道 |

## 4. 跨框架考量（K-xx）

| 编号 | 考量 | 出处 | 对 OMR 的含义 |
|---|---|---|---|
| K-01 | 设备层只回答"芯片几何与擦写语义"：读可任意，**擦除整扇区、写有 program 粒度与对齐约束**；接口显式暴露 geometry，不默认 | CMSIS / Zephyr / FAL | FlashDev 必须带 geometry 查询（capacity/扇区布局、program 粒度、erase_value）；总线映射基址**不进几何**（2026-09-06 裁决 A） |
| K-02 | 物理设备 ≠ 逻辑分区，分层放置：分区 = 静态描述表（名字/设备/偏移/大小），上层只认名字与偏移，永不裸地址 | Zephyr flash_map / FAL | 分区表独立成模块；换芯片不动分区表 |
| K-03 | NOR 只能擦后写、原地更新 = 自毁 → **staging 槽位先行**是全部方案的共同骨架：staging → 校验 → 激活 | 全部 | 任何升级路径必须先落到非启动区，校验通过前不得触碰运行区 |
| K-04 | 激活 = 翻转一个小子段指针/状态，**不是搬移**；搬移只发生在 bootloader 提交阶段（swap）而非每次升级 | ESP otadata | A/B + last-good 指针优先于"每次升级全量搬移" |
| K-05 | meta 掉电安全 = **双份轮转 entry + seq + magic + CRC**：半写 entry 因 CRC 失败被忽略，读一个 entry 即得真相 | ESP otadata / MCUboot trailer | boot 决策必须"读到一个小结构就够"，不在启动时解析整镜像 |
| K-06 | intent 先行：状态机**每推进一步先写状态再动手**（journal 思想）；每步幂等、掉电任意点可判定 | MCUboot trailer | OTA/swap 状态机推进准则 |
| K-07 | **bootloader 是升级事实源**；app 无权自改启动决策（它可能已坏）。状态语义只有三种：pending（试一次）→ confirm（正式）→ 回退 | ESP / MCUboot | 确认接口语义定死；app 只做"我活了，确认" |
| K-08 | **自动回滚窗口**：pending 镜像在 N 次启动或看门狗超时内未确认 → 自动回退旧镜像/旧槽位 | ESP | 联调"刷了新固件起不来且人不在"也可自愈 |
| K-09 | 整镜像校验先于激活；**镜像头自校验 + 带结构版本号 + 预留字段**（防新旧 bootloader/镜像双向错位，可优雅拒绝） | MCUboot / ESP | 镜像头契约 = bootloader 第一交付物 |
| K-10 | 传输与安装解耦；**续传点由接收侧记录**（慢通道断了重来是灾难） | mcumgr / ESP | 协议面（块传输/重传/续传）与安装面（staging 写入）分离 |
| K-11 | bootloader 薄：无动态内存、无复杂驱动栈、启动路径确定性；**策略由构建/配置期固定**，运行时不做策略切换 | MCUboot / OpenBLT | log 三件套支撑"bootloader 也有日志"；每 binary 单策略 |
| K-12 | 保留**后门进入通道**（串口握手/按键/超时/升级标志），单区方案在镜像无效时仍能收固件——防砖 | OpenBLT | bootloader 必须能主动进入下载模式，不能只被动等 |
| K-13 | 权力分离：**写 staging = 下载方职责**（app 内 OTA 服务或外部下载器），**提交（翻转指针/执行 swap）= bootloader 权力** | ESP / MCUboot 流程一致 | OTA 状态机归属据此划分：app 写 staging + 标 pending；bootloader 决策执行 |
| K-14 | 并发锁模型：锁粒度 = 设备（物理片/擦写引擎）粒度，**无"设备内子锁"先例**；两派 = API/驱动内建可睡眠锁（ESP-IDF 全局互斥、MTD chip/controller 双锁、Zephyr spi-nor k_sem 全操作排他）vs 调用方纪律（Zephyr flash API 无锁、FAL 零互斥、CMSIS 隐含单飞行）；**ms 级写擦用可睡眠锁度过、不关中断**（ESP 禁 cache/禁中断系其 cache 架构被迫，非普遍）；FAL 被社区在 port 层补锁 = 纪律模型在 RTOS 并发下会漏的实证；ESP 承认 v4.0 起并发读可读新旧混合数据 | ESP-IDF / Linux MTD / Zephyr spi-nor / FAL | 双层锁落地（v1）：device 锁管本片时序；共享总线锁仅传输瞬间（Linux spi-nor 同款）；写/擦异步化（单飞行 slot + 完成源），read 同步保留（F-04/F-06 v1 修订——v0"可睡眠互斥全排他同步"经真机实测废弃：忙等饿死低优线程） |
| K-15 | 设备对象判据：设备 = 物理"片/擦写引擎"（锁与擦除的根）；**bank 一般不构成设备判据**——Zephyr 整片一设备（F4 驱动作者明示权衡过拆两设备而不做）、MCUboot 槽位可连排同 bank 且 bootutil 不感知 bank、FAL 分区绑定单设备不可跨；要跨片/bank 并发的自然表达 = 拆设备或 mtd_concat 拼合，**非子锁**；ESP 每物理片一 chip、外片因 cache 禁 APP | Zephyr / MCUboot / FAL / ESP-IDF / Linux MTD | F407 双 bank = 一个 flash0，bank 入几何区域表；拆设备路径留口（F-05） |
| K-16 | 完成源边界与事件范式裁决（2026-09-06 三方调研收敛）：**flash 框架 API 对调用者保持同步，异步完成源压驱动/后端用信号量吸收**——Linux 2018 删 mtd_erase 假异步（"None of the mtd->_erase() implementations work asynchronously"）；Zephyr flash async PR（HAL_IT+IRQ）被否（作者自承"称 async 有误导性"——调用线程仍阻塞）；CMSIS SignalEvent 规格完备但实际消费近零（ARM 自家 FileSystem 把 READY 事件写进无人读取的字段，等待一律 GetStatus 轮询）；**bootloader 生态（MCUboot/OpenBLT/ST ROM/TI BSL/NXP）全为同步阻塞 + 轮询 + 拆片 + 主机长超时**（OpenBLT erase 超时默认 10s、ST AN3155 承认秒级静默、MCUboot 喂狗在扇区间且默认容忍 5 分钟）；无 OS 复用手法 = 编译期把 OS 原语裁剪为空转（U-Boot compat.h 样板）。**2026-09-07 修订注记**：含义列"裸机 osal 空桩/空转"表述经用户拍板细化——空转宏（U-Boot compat.h）为历史妥协，现代做法 = 按使用面剔除 + 同步面保真（见 K-19），本列以 K-19 为准 | Linux MTD（commit 884cfd90）/ Zephyr（PR #93173）/ CMSIS + ARM fs_nor_media / MCUboot loader+bootutil_area / OpenBLT / ST AN3155 / TI SLAU887 / U-Boot compat.h | FlashDev 保持"同步 API + 域 worker + 后端完成源可插拔（EOP 中断主路径/轮询退化，D-07）"；**事件不升框架 API**（提交者是任务时同步即充分；能耗用阻塞+tickless 解决而非事件化）；bootloader 复用：带 OS 用同步等待原语 / 裸机走 osal-none（ADR-0022 (osal_none_bare_metal)：同步面保真 + 单执行流 + async 随 OM_FLASH_SYNC_ONLY 编除，K-19）；"同步擦写 + 让出"是框架下沉原语。**2026-09-16 修订注记**：句中"async 随 OM_FLASH_SYNC_ONLY 编除"经实核不成立——`OM_FLASH_SYNC_ONLY` 为幽灵宏（全仓零代码消费），真实形态开关 = OS 轴 `OM_OSAL_PORT == OSAL_PORT_NONE`，且该轴下形态为**同步直跑**（提交即执行）而非"async 返 NOT_SUPPORTED"；以 ADR-0022 修订注记与 `osal_none_core_design.md` A8 为准 |
| K-17 | 镜像校验链实践（2026-09-07 调研）：完整性/真实性分层——**CRC32（错误检测，随机损坏防护）是无签名链场景的行业底线**（OpenBLT 默认 CRC32、ST/TI ROM loader 无整镜像摘要）；**SHA-256 在成熟框架中总是签名链的输入而非独立选择**（MCUboot digest→ECDSA 验签、ESP 镜像尾 SHA-256 服务 secure boot 签名、U-Boot FIT hash 配签名）；无签名配套时 SHA-256 相对 CRC32 无真实安全增量（篡改者可重算摘要）；结构设计 = **摘要算法 ID + 摘要区按算法预留**，头契约版本化保证旧侧遇新算法优雅拒绝 | OpenBLT / MCUboot / ESP-IDF / U-Boot FIT | 镜像头契约带 digest 算法 ID、bootloader 侧校验模块按构建期选型编译——CRC32 起步，SHA-256+ECDSA 演进不动契约（Q-04 拍板）。**2026-09-14 修订注记**：出处列"OpenBLT 默认 CRC32"经实核证伪——OpenBLT 默认校验值 = app 向量表内固定偏移处前 7 个异常向量地址之和的二进制补码（`BOOT_FLASH_VECTOR_TABLE_CS_OFFSET`），整片 CRC-16 属**商业 Add-on（不开源）**；"CRC32 = 无签名链场景的行业底线"这一**定位不变**，但生态参照不是 OpenBLT（见 K-28） |
| K-18 | 回退窗口与提交策略强耦合（2026-09-07 调研）：**swap 系天然回滚**（旧镜像物理在场，提交中断即退/续——MCUboot，无启动计数）；**direct 系自动回退 = 启动计数**（ESP-IDF otadata：未 confirm 每启动 +1，达限翻回旧槽）；MCUboot direct-xip 明示无自动回退；看门狗超时属另一派（app 侧外设依赖重） | ESP otadata / MCUboot loader | direct 策略配启动计数 N 次（Q-06 拍板，无看门狗依赖）；swap 后续落地时回退语义随策略天然获得，骨架不重复造回退机制。**2026-09-14 修订注记**：本条含两处事实错误，经实核更正——① "ESP-IDF otadata 未 confirm 每启动 +1、达限翻回旧槽"不成立：ESP **无数值型启动计数**，`ota_state` 为一次性语义（NEW → 首次启动 PENDING_VERIFY → 下次启动仍为它则判 ABORTED），**上限恒 1 次**，且改判发生在选槽之前故自然回退；RTC 中的 `reboot_counter` 掉电即失、用途为深睡快启，不参与回退。② "MCUboot direct-xip 明示无自动回退"不准确：direct-xip 具 `MCUBOOT_DIRECT_XIP_REVERT`，回退动作 = **擦除未确认的槽**，粒度 1。**推论：Q-06 的"启动计数 N 次"是框架增强而非行业标准**，其设计依据见 K-25 |
| K-19 | 裸机 osal 语义参考（2026-09-07 调研，用户指名 XRobot）：**同步原语在单执行流下保语义有直接先例**——XRobot/LibXR：统一系统 API（sem/thread/queue/mutex/timer）裸机全实现，等待路径驱动 idle 刷新（RefreshTimerInIdle），ISR 只交接不阻塞（FromISR 原语纪律与我们同语言）；FreeRTOS 协作模式 / Zephyr coop 佐证 **mutex/sem 语义与抢占无关**（表达资源/事件状态 + 临界区纪律）；任务化谱系 = 协作多任务（独立栈）/ Protothreads（共享栈）/ QP 活动对象 / 单执行流 + 软定时器（XRobot 裸机形态）；mbedTLS threading-alt 与 SFUD = "按面编译剔除"先例（非空转占位）。**修订 K-16 含义列**：U-Boot 空转宏为历史妥协；按面剔除 + 同步面保真为现代做法 | XRobot/LibXR / FreeRTOS 协作模式 / Zephyr K_COOP / QP / Protothreads / mbedTLS threading-alt / SFUD | osal-none 端口 = 时间面（SysTick/DWT）与中断面（irq_lock）保留 + 同步面保真（mutex=0/1 互斥开关、sem=生产消费计数、等待由 idle 刷新驱动——基于 osal 的 sync 组件裸机行为一致）+ 单执行流首版（thread 创建 NOT_SUPPORTED，async 随 OM_FLASH_SYNC_ONLY 编除）；协作式多任务记演进（ADR-0022 (osal_none_bare_metal)）。**2026-09-16 修订注记**：thread 创建与 async 的实际行为以 ADR-0022 修订注记为准——直调占位 / os 轴同步直跑（宏 `OM_FLASH_SYNC_ONLY` 不存在于代码） |
| K-20 | 槽位映射生态（2026-09-07 调研）：**成熟框架无"remap 框架 API"**——槽 = 静态表（Zephyr flash_map / 分区表，构建期固定）；启动 = 校验 + 跳转（MCUboot direct 固定 XIP 地址：设 VTOR + 跳入口，无 remap 动作）；真 remap（bank swap / MMU 重映射）为芯片级跳转前动作——ST 双 bank IAP（swap 位/option）、ESP 外部 flash MMU 在 ROM/bootloader 内消化 | MCUboot + Zephyr flash_map / ESP-IDF / ST 双 bank IAP | P-04 落点印证：板适配只提供"槽→物理"静态表 + 跳转动作；rm-a direct = 固定地址跳转（remap 平凡情形）；bank swap 类若未来使用 = 板适配跳转前动作，不产生框架 API（Q-10 拍板） |
| K-21 | 下载通道命令组架构（2026-09-07 用户拍板）：**bootloader 只认镜像**（不care存储点与来源）；传输介质（UART/CAN/USB）与下载逻辑解耦的成熟形态 = **命令组 + 多介质 transport**（mcumgr SMP：Mgmt 组 ID 分发 + Transport 层多后端 serial/BLE/shell + img_mgmt 命令组载荷=镜像——上传 staging/test/confirm；MCUboot 不感知介质）。用户方案与拍板：**通用通信/命令服务**（services 层）承载命令组，boot 组（载荷=镜像）为其一组；bootloader 首版后门协议**按命令组形态设计**（骨架 = 该服务的裁剪前身，一组一命令 boot_upload，UART 第一介质后端）；服务本体记框架演进项（与 log v4 shell 规划合并），boot 组届时迁入。**boot 组为高危命令组**（载荷覆写镜像）——访问门禁（握手时序/口令/窗口）为设计约束，门禁位置（服务通用机制 vs boot 组自持）实现期定 | mcumgr SMP / Zephyr shell | 后门协议 = 命令组形态（非串口私有协议）；OTA 与后门共用协议语义（介质无感）；bootloader 侧裁剪子集面积预算须验证（K-11 64K）；Q-08 拍板语义扩展（串口 = 第一介质后端） |
| K-22 | 镜像头契约的成熟形态（2026-09-14 实核）：MCUboot 头 32B（magic / load_addr / **hdr_size** / protect_tlv_size / img_size / flags / 版本 8B 四段 / 填充），**hdr_size 即负载偏移**（典型 0x200，为满足 STM32 向量表对齐；工具侧只设下界 32）；TLV 区在**负载之后**（`BOOT_TLV_OFF = hdr_size + img_size`），摘要不覆盖自身故无循环；**MCUboot 无结构版本字段**——不兼容靠 magic 表达（`IMAGE_MAGIC_V1 0x96f3b83c` 为先例）。ESP-IDF 头 24B 且**无负载偏移/总长字段**（靠段链表顺序解析），校验 = 1 字节 XOR 折叠（落 16B 补齐块末字节）+ 可选追加 SHA-256；兼容性防护用 chip_id/min_rev 并在不匹配时拒绝。OpenBLT 校验值在 **app 向量表内固定偏移**。U-Boot legacy uImage 64B 头带 **hcrc + dcrc 双 CRC**，hcrc 范式 = 拷贝头 → hcrc 字段清零 → 算 64B → 比对 | MCUboot `bootutil/image.h:167-176` + `imgtool/main.py:344-349` + `bootutil_img_hash.c:122-135` + `image_validate.c:164-194`；ESP-IDF `esp_app_format.h:82-112` + `esp_image_format.c:1066-1088`；OpenBLT `Target/Source/ARMCM4_STM32F4/flash.c:316-406`；U-Boot `include/image.h:325-338` + `boot/image.c:230-243` | 契约头 = 定长头 + **固定尺寸摘要区**（256B 可容纳 SHA-256 + ECDSA-P256 签名 + keyhash，预留恒定故新旧两侧无需重协商）；**hdr_version 是相对 MCUboot 的增强**（MCUboot 只能靠换 magic 表达不兼容）；**不设独立头 CRC**——direct-xip 下活动槽头永不重写、半写路径不存在，且 hcrc 无认证能力不能替代摘要（字段保留、置零 = 未启用）；**尺寸校验必须先于任何基于头内尺寸的访问**（头尚未被认证） |
| K-23 | 镜像布局中**三个独立的对齐量**（不应合并为单一 align 字段）：① 负载偏移 = CPU 向量表对齐（MCUboot 典型 0x200，非写粒度）；② trailer 写粒度 = `BOOT_MAX_ALIGN`，且被**编码进 16B magic 的首 2 字节**以自描述；③ 槽基址对齐 = ESP-IDF 对 app 分区强制 64KB，未对齐由构建工具直接报错；另有"校验字必须落在首个可写块内"这类由介质几何施加的约束 | MCUboot `docs/design.md:667-700`；ESP-IDF `partition-tables.rst:218-222`；OpenBLT `flash.c`（`#error` 强制） | 三者分别表达：负载偏移决定 **app 链接基址 = 槽基址 + payload_offset**（0x10200 / 0x110200，均为 0x200 倍数）；槽基址对齐由分区表注册期几何校验承担；写粒度由 FlashDev 几何提供 |
| K-24 | boot 状态小块的三方形态与写序铁律（2026-09-14 实核）：**MCUboot** = 槽尾 trailer，**无结构校验和**（16B 常量 magic + 单字节标志的组合表，magic 永远最后写）；**ESP** = 独立 otadata 双扇区轮转，CRC32 **只覆盖 ota_seq 4 字节**（state 不受保护），取 seq 最大者为活动项，**写谁擦谁、从不擦另一份**；**nRF5** = 独立 settings 页 + backup 页，CRC 在 offset 0 覆盖头段，erase → 整结构一次 store，主坏用备、都坏清零重建 | MCUboot `bootutil_priv.h:159-201` + `swap_misc.c:106-156`；ESP-IDF `esp_flash_partitions.h:76-83` + `bootloader_common_loader.c:72-96` + `bootloader_utility.c:307-316`；nRF5 `nrf_dfu_settings.c:158-277,313-364` | meta = 独立区两扇区**各放一份 entry**；三条铁律——**提交标记（magic）最后写**、**永不擦当前有效副本**、**计数器放 entry 本体**；entry 有效性 = magic 命中 && CRC 通过，且 erase 态（全 0xFF）须**显式判无效**（nRF5 `crc != 0xFFFFFFFF` 先例），不能只靠"算出来不等" |
| K-25 | 启动计数的生态事实（2026-09-14 实核，Q-06 的定位依据）：**ESP-IDF 无数值型启动计数**——`ota_state` 一次性语义（NEW → 首次启动 PENDING_VERIFY → 下次启动仍为它则判 ABORTED，上限恒 1，改判在选槽之前故自然回退）；MCUboot direct-xip revert = **擦掉未确认的槽**（粒度 1）；nRF5 无 boot count（bank_code 二值）；Zephyr 无内建 boot count（retention/bootmode 只存 1 字节模式且落 retained memory 而非 flash） | ESP-IDF `bootloader_utility.c:394-401` + `bootloader_common.h:223-271`；MCUboot `loader.c:2149-2206` + `design.md:508-537`；nRF5 `nrf_dfu_types.h:166-180`；Zephyr `retention/bootmode.c:11-43` | **Q-06 的"启动计数 N 次"是框架增强，生态上限为 1 次**——须自行完整设计：计数落 entry 本体、**仅在 PENDING 时推进**、**跳 app 前落盘**（掉电/复位天然推进），稳态 VALID 零 flash 写；达限时**先校验另一槽**，合法才写新 entry 翻指针，不合法则停后门（绝不写一条指向坏镜像的 entry） |
| K-26 | 下载协议的帧同步与续传语义（2026-09-14 实核）：SMP 帧头 8B（op/flags/len/group/seq/id；len 与 group 为网络序；一包可串多请求、载荷 4 字节对齐）；img_mgmt `upload` 载荷 {image, data, len(整镜像总长), off, sha, upgrade}——**续传点由接收侧持有**、响应恒回带期望 off、**off 不匹配 = 忽略本块并回正确值**、off==0 且 sha 相同 = 续传（sha 允许截断，客户端负责防碰撞）；**无 scratch**，直写目标槽（可选惰性/渐进擦除）。串口 transport = 换行定行 + 双字节标记（0x0609 / 0x0414）+ base64 + CRC16-ITU（整包校验和为 0）；raw 模式无标记无 CRC，**靠输入静默超时防卡死**（官方文档明说否则会 wedge）。MCUboot serial recovery 复用同一 SMP/img_mgmt 语义、默认写 primary 槽、`curr_off` **只在 RAM**（复位即失）、进入 = GPIO 或 500ms 等字节窗口、退出 = 回 ACK → 延时 250ms → 冷复位（**不复用返回路径**，官方注释：返回路径会锁死 M0+）。AN3155 = 无帧头、命令 + 反码、ACK 0x79 / NAK 0x1F、XOR 校验、支持任意有效地址但无槽概念。XMODEM/YMODEM = SOH/STX + 块号 + 块号反码 + CRC16，重试上限 10 次；YMODEM 块 0 带文件长度但**协议本身无"从偏移 N 开始"**。OpenBLT/XCP = 帧内零长度零 CRC（边界由介质层给），**目标不记断点**（续传推给主机） | mcumgr `mgmt.h` + `smp.c` + `img_mgmt.c`；Zephyr `serial_util.c` + `uart_mcumgr.c` + `smp_raw_uart.c`；MCUboot `boot_serial.c` + `Kconfig.serial_recovery` + `docs/serial_recovery.md`；ST AN3155 §3；Forsberg X/YMODEM 1988；OpenBLT `xcp.c` / `com.c` / `backdoor.c` | 后门 v0 帧 = **双字节 SYNC + 长度 + CRC16 + 半包超时**（二进制 UART 不用 base64——那是文本通道的代价）；入帧顺序 = 字符级重同步 → **长度上界预检（绝不按 LEN 分配缓冲）** → CRC 终检；上传语义照抄 img_mgmt（off 由接收侧记录并**持久化**——明确不采用 MCUboot 的 RAM-only `curr_off`）——使后门与将来 app 侧 OTA 共用同一命令语义；流控 = 停等 + SEQ 去重（重复 SEQ 重发上次响应）+ T_ACK 覆盖最坏擦除时间；退出需延时后复位 |
| K-27 | 跳转序列与"同一份源码、各自构建"（2026-09-14 实核）：MCUboot 端口层 `do_boot()` = 条件关 SysTick → 关会对 app 未就绪栈打中断的外设（USB；注释原文 "prevent it from firing interrupts into the application before it has brought up its own stack"）→ `cleanup_arm_interrupts()`（**只清 NVIC 的 ICER/ICPR，不碰外设寄存器、不清 SysTick/PendSV 悬挂位**）→ CONTROL=0 → 设 VTOR（**仅当配置要求**）→ 无条件设 MSP → 跳转。ST 官方 IAP 只做"MSP + 跳转"（VTOR 由 app 自己的 SystemInit 设，注释要求 0x200 倍数）。**MCUboot 明确不支持位置无关镜像**（`IMAGE_F_PIC` 标 Not supported），direct-xip 要求"镜像链接到各自的槽"——即**同源码构建两次，而非同一个二进制** | MCUboot `boot/zephyr/arch/arm.c` + `cleanup/arm_cortex_m.c` + `Kconfig:1161-1167` + `design.md` Limitations；STM32CubeF4/F1/F7/L0 的 IAP 示例（四份一致）；Zephyr `prep_c.c` + `vector_table.ld` | 跳转清单 = 关中断 → 清 NVIC 使能/pending → 清系统异常悬挂 → 关 SysTick → **bootloader 用过的外设关中断使能位 + 读 DR 清标志** → CONTROL=0 → VTOR（+DSB/ISB）→ 设 MSP → 跳；A/B 双槽 = 构建期变量各构建一次（链接基址与 VTOR 随槽变）；**PRIMASK 的放开责任方须在契约里定死**（否则 app 可能永远收不到中断） |
| K-28 | 校验算法的跨平台一致性（2026-09-14 实核）：F4 硬件 CRC = 固定多项式 + 固定初值 + **无反射、无最终 XOR**、只能写 32 位字、每字 4 HCLK，且**字内字节序被反转**（实机观测 0xF407A5C2 → 0xB5E8B5CD），等价"每 4 字节组反转字节序的 CRC-32/MPEG-2"；可编程 CRC 外设（F7/H7/G4/L4+ 等具 REV_IN/REV_OUT/XOROUT）可配置成 IEEE 并可 DMA 直喂；F4 **无 CRC DMA 请求线**（仅 DMA2 的 MEM2MEM 可绕过，而 DMA 不能做逐字变换）。**MCUboot 全版本不用 CRC32**（用 SHA-256 摘要 TLV）；OpenBLT 默认用前 7 异常向量补码。960K 校验耗时：硬件 6–10ms / 软件表驱动 ~58ms / 软件 SHA-256 ~280–360ms；**F427 无 HASH 外设**（SHA-256 演进只能走软件） | RM0090 §4 + AN4187 Table 1/2/4 + Renode issue #585；Zephyr `drivers/crc/crc_stm32.c`；MCUboot `image_validate.c`（各版本 grep 无 crc）；OpenBLT `flash.c`；STM32F427 datasheet | **框架定义 canonical 变体，芯片后端负责映射**（换芯片 = 换适配器）：canonical = CRC-32/ISO-HDLC；F4 后端 = 硬件 + 逐字 `__RBIT(__REV(w))` + 末尾 `__RBIT(DR) ^ 0xFFFFFFFF`（~8.6ms/960K，**不可 DMA——硅片限制而非架构取舍**）；可编程 CRC 芯片 = 硬件原生 + DMA；PC 工具侧 = `zlib.crc32`。**digest 算法 ID 命名 canonical 变体而非任何后端口径**；摘要覆盖区须 4 字节对齐且长度为 4 的倍数 |
| K-29 | 启动校验策略与面积实证（2026-09-14 实核）：**生态默认每次上电校验整镜像**——MCUboot `BOOT_VALIDATE_SLOT0` default y（逃生项 `..._ONCE` 把结果缓存在 magic 区，官方自评 unsecure）；direct-xip 每次启动校验将启动槽的**整镜像摘要**（非仅头）；ESP-IDF 默认整镜像校验，跳过开关全 default n 且官方措辞为慎用。面积实证：MCUboot hash-only ≈ 19KB（Cortex-M33 平台）/ 默认带 RSA-2048 ≈ 32.9–40.1KB（Cortex-M4）/ 加串口恢复 ≈ 46.6KB；OpenBLT 单介质 ≈ 4.6KB（F4 上受扇区限制须预留 16KB） | MCUboot `boot/zephyr/Kconfig`（`BOOT_VALIDATE_SLOT0` / `..._ONCE`）+ `loader.c`；ESP-IDF `Kconfig.projbuild`；MCUboot PR #2015 构建日志；Renesas R11AN0869EU0100；OpenBLT FAQ + 端口手册 + 博客 | 每次上电校验**整个待启动镜像**（硬件 CRC 6–10ms 属零成本），**不引入 magic 缓存类削弱机制**；64K 预算充裕（≈2.5×），真正吃面积的是 SHA-256+ECDSA 演进（≤40KB 量级）——**从第一天起做面积看门狗**（构建后 size 报告） |
| K-30 | 决策数据（meta）布局与写序（2026-09-16 实核）：**ESP-IDF otadata** = 双 4KB 扇区各一条 32B entry（`ota_seq` u32 / `seq_label[20]` 未使用 / `ota_state` u32 / `crc` u32），**CRC 只覆盖 `ota_seq` 4 字节**（头文件原文注释 "CRC32 of ota_seq field only"——`ota_state` 不受保护）；有效性 = seq ≠ 0xFFFFFFFF 且 state ∉ {INVALID, ABORTED} 且 CRC 通过；两份皆有效取 seq 大者；**写序 = 先擦目标份扇区再整条写 32B，永不擦当前有效份**（擦后写前掉电 → 全 0xFF → 判无效 → 旧决策胜出，失败方向 = 不切换）；每次状态跃迁 = 1 次扇区擦 + 32B 写。**U-Boot 双份 env** = 两份各占一扇区，CRC 只覆盖 data（不覆盖 flags），1 字节 `flags` 承担 seq 语义（ACTIVE=1 / OBSOLETE=0），写序 = ①擦待写副本 ②写整份 ③**只写 1 字节**把旧副本标 OBSOLETE ④交换指针。**MCUboot trailer** = 槽尾，16B magic **永远最后写**（"magic is always written in the last step"，据此"magic 有效 ⇒ 其余元数据有效"），`image_ok`/`copy_done` 为 **1 字节 write-once**（0xFF→0x01，无需擦除）。**追加式（在已擦扇区内按偏移顺序写记录、写满才轮转擦另一份）是 NOR 通用形态**：program 只能 1→0、擦除才恢复全 1，故往从未写过的 0xFF 偏移写**不需要擦除**；其约束 = 记录长度/起始偏移按 `writeUnit` 对齐（F4 = 4B 字）、带 ECC 整页写约束的器件需按页缓冲 | ESP-IDF v5.2 `esp_flash_partitions.h:59-66` + `bootloader_common_loader.c:33-46,95-107` + `bootloader_utility.c:367-376,417-423` + `esp_ota_ops.c:389-452,759-803`；U-Boot master `env/common.c:473-550` + `env/flash.c:126-211` + `include/env.h:62-63`；MCUboot v2.4.0 `bootutil_priv.h:159-200` + `bootutil_public.c:302-374,546-552` + `swap_misc.c:35-75`；F4 几何 `bsp_flash_f4.c:54-62`（writeUnit=4 / erasedValue=0xFF） | meta = **独立区双份、每份一条定长 entry、整写式**（Q-12）；照抄生态两处并改进一处——**CRC 覆盖决策字段全集**（ESP 只护 4 字节 seq 的教训）、**提交标记最后写**（MCUboot magic 先例）、**永不擦当前有效份**（K-24 铁律在生态的原型）。追加式的收益在"**擦除次数**"口径成立（≈0～1 次/OTA vs 整写式 N+2 次），但"一个擦除周期内 program 多少次不损及寿命/数据保持"**无厂商原文背书**——记演进并附三条前置条件（见 §5.1）。**2026-09-16 修订注记**：①"独立区**双份**"的份数结论已被推翻——擦除归属改引导侧后**至少需 3 份**（推演见 Q-12 行与 §5.1），最终份数待定；②"一个擦除周期内 program 多少次不损及寿命/数据保持"经实核**已有官方背书**（ST AN3969 §3.4.1 / AN2594 §3.4："A program/erase cycle consists of one or more write accesses and one page erase operation"），但 **retention 维度仍无背书**——见 K-38；③"先擦目标份扇区再整条写"是 ESP 的形态，OMR 已改为"**只 program 已擦副本、擦除归引导侧**"（K-36） |
| K-31 | 启动计数落盘范式（2026-09-16 实核，Q-06 的落地依据）：**只有 U-Boot 有数值型启动计数**，且 `bootcount_store()` 与 `bootcount_load()` **以 `upgrade_available` 为唯一开关**（标志为 0 时 store 什么都不做、load 直接返回初值 0）——即"**只在未确认窗口落盘**"；`bootlimit` 语义 = 超限改跑 `altbootcmd`（非自动回滚），`0` 即禁用。ESP-IDF 与 MCUboot 均为一次性语义（上限恒 1 次，K-25）。confirm 形态：MCUboot = 写一次 1 字节 `image_ok`（**幂等、无需擦除**，`image_ok == UNSET` 才写）；ESP = 写 `ota_state=VALID` 并重写 seq（**一次扇区擦**）；ESP 稳态零写的实现 = `state != VALID` 才写 | U-Boot master `drivers/bootcount/bootcount_env.c:9-17` + `bootcount.c:14-35` + `include/bootcount.h:102-117`；MCUboot v2.4.0 `bootutil_public.c:734-751` + `:546-552`；ESP-IDF v5.2 `esp_ota_ops.c:759-803` | 计数**仅在 state ∈ {NEW, PENDING} 时推进**、**跳 app 前落盘**（掉电/复位天然 +1，R-SLOT-7）、`state == VALID` 时**完全跳过写**（R-SLOT-8 稳态零写）；confirm 是**一次性写**（此后到下次 OTA 零写）；计数 +1 写入时**顺带 seq+1**——每次计数写天然成为一条新决策记录，"哪份新"永远由 seq 决定，省掉独立的计数比较逻辑 |
| K-32 | 跳转动作清单与 ST 官方形态（2026-09-16 实核）：**ST 官方 F4 IAP 例程是"裸跳"**——只有 `__set_MSP(*(uint32_t*)APP_ADDR)` + 复位向量函数指针调用，**无**关中断 / 无 `HAL_DeInit()` / 无停 SysTick / 无任何 NVIC 清理（8 个系列例程全文件扫描命中 0），清理责任推给 app 的 `SystemInit()`。完整清单来自 **MCUboot-Zephyr**（`cleanup_arm_interrupts()` = ICER 全清 + ICPR 全清；`sys_clock_disable()`；CONTROL=0；条件设 VTOR；无条件设 MSP）与 **OpenBLT**（`HAL_DeInit()` → `TimerReset()`（SysTick CTRL/LOAD/VAL=0 + 清其 pending）→ 写 VTOR → 取复位向量 → `cpsie i` → 跳）。**权威文档纠错**：VTOR 与 NVIC 等内核外设**不在 RM0090**（Rev21 全文 0 命中 `VTOR`/`E000ED`，封面明示内核部分见 PM0214），对应内容在 **PM0214**；PM0214 两条原文决定清单必要性——"An interrupt can enter pending state even it is disabled"（故必须清 ICER/ICPR，不能只关 PRIMASK）与 "Before programming VTOR … ensure the vector table entries of the new vector table are setup for fault handlers, NMI and all enabled exception"。**另**：ST 官方 F4 IAP 例程对应 **AN3965**（非 AN4657） | RM0090 Rev21（全文检索）+ PM0214 Rev4 §2.3.7 / §4.3.3 / §4.3.5 / §4.3.10 / §4.4.4；STM32CubeF4 `Projects/STM324xG_EVAL/Applications/IAP/IAP_Main/src/main.c:70-79`（readme 头部引 AN3965）；MCUboot v2.4.0 `boot/zephyr/main.c:184-243,292-301` + `cleanup/arm_cortex_m.c:15-29` + `Kconfig:490`；OpenBLT v1.22.0 `Target/Source/ARMCM4_STM32F4/cpu.c:75-158` + `timer.c:109-122` | 跳转清单（Q-13）= 关中断 → ICER/ICPR 全清 → 停 SysTick 并清其 pending（**必做**：ST HAL `HAL_Init()` 默认开 1ms SysTick 中断，而 `bsp_cpu.c:91` 正会调它）→ `HAL_DeInit()` → 写 VTOR → 装 MSP → 清 CONTROL → 放开中断 → 跳；**ST 例程不可照抄**——它假设自己从复位态进来（NVIC 全禁、无残留外设），该前提在 IAP 链路上不成立 |
| K-33 | 中断责任方两案与看门狗事实（2026-09-16 实核）：**方案 A（bootloader 跳转前放开）** = OpenBLT，注释原文 "The Cortex-M4 core has interrupts enabled out of reset. the bootloader explicitly disables these for security reasons. Enable them here again, so it does not have to be done by the user program."（先 `HAL_DeInit()`/`TimerReset()`/写 VTOR，再 `CpuIrqEnable()`，最后跳）。**方案 B（app 早期放开）** = MCUboot+Zephyr 主流（MCUboot `boot/zephyr/` 全目录 `__enable_irq` 0 命中，跳转前保持屏蔽；Zephyr 复位即锁、注释 "will get unlocked when switch to main task"，放开点 = `arch_switch_to_main_thread()` 内 `arch_irq_unlock(0)`）；ESP-IDF（Xtensa，拓扑参考）bootloader→app 路径既不关也不开、app 侧按需逐线放开；ST 例程属"无人关故无需开"的退化形态。**看门狗**：F427 的 IWDG **一旦启动无法软件关闭**（HAL 仅 `HAL_IWDG_Init`/`HAL_IWDG_Refresh` 两个函数、无 DeInit；选项字节 `WDG_SW=0` 时上电即在跑）；生态在跳转点**均不碰狗**（MCUboot 只喂不关、默认超时 300000ms；OpenBLT `CpuStartUserProgram()` 零看门狗动作；ST 例程无看门狗）；唯一例外 = MCUboot Cypress 端口**跳转前显式开狗**并把"app 不喂 → 复位 → 回滚"当回退触发器（注释成文）；ESP-IDF 用 Kconfig 明文要求"app 必须显式 reset/feed/disable" | OpenBLT v1.22.0 `Target/Source/ARMCM4_STM32F4/cpu.c:132-149`；MCUboot v2.4.0 `boot/zephyr/main.c:184-243` + `Kconfig:490,1105-1111` + `boot/cypress/MCUBootApp/main.c:140-151`；Zephyr v4.1.0 `arch/arm/core/cortex_m/reset.S` + `thread.c`（`arch_switch_to_main_thread`）；ESP-IDF v5.4.1 `bootloader_init.c:64-94` + `Kconfig.projbuild:288-302`；STM32F4 HAL `stm32f4xx_hal_iwdg.c:30-35,164,238` + `stm32f427xx.h:7174-7176`；OMR 现状 `bsp_cpu.c:91`（`HAL_Init()`）、`stm32f4xx_hal_conf.h:58,72`（IWDG/WWDG 模块为注释态） | **Q-13 采纳方案 A**：app 侧零改动、与"复位默认 PRIMASK=0"的既有隐含契约一致，os=none 与 FreeRTOS 两形态通吃；方案 B 的代价 = os=none 下**今天不存在放开点**（`osal_kernel_start()` 是 no-op）且 pre-scheduler 段的中断语义需重新定义。**Q-14 首版不启用看门狗**（OMR 当前无任何看门狗驱动）；若将来启用，"谁喂"必须写进契约——两处成文先例（MCUboot Cypress、ESP Kconfig）都表明它不能留给隐式约定 |
| K-34 | 防降级的两条轴与代价（2026-09-16 实核）：**软件版本比较轴**——MCUboot `MCUBOOT_DOWNGRADE_PREVENTION`（Kconfig choice 带 `optional`，**默认不开**，不开时相关代码整段不编译）；纯版本比较版 **`depends on !BOOT_DIRECT_XIP`**，即 **direct-xip 下不构成"拒绝降级"**（其默认行为是 `find_slot_with_highest_version()` 在可用槽中**选最高版本**）；比较字段 = major/minor/revision，`build_num` 默认**不参与**（需 `BOOT_VERSION_CMP_USE_BUILD_NUMBER`）。**硬件计数器轴**——`MCUBOOT_HW_ROLLBACK_PROT`：镜像内 `IMAGE_TLV_SEC_CNT`（0x50，位于 **protected TLV 区**，"must always be present"，缺失即整镜像校验失败）+ 设备侧单调 NV 计数器（接口 `boot_nv_security_counter_*`；上游只给接口，落点实现自备；接口注释要求"能从中断的递增中恢复、稳定态不可因任何掉电下降"）；**强度取决于签名**——protected TLV 只被摘要覆盖，无签名时摘要可被重算；失败动作不是"静默不启动"而是**销毁被拒镜像**（`boot_scramble_slot`）。ESP-IDF 的 eFuse 路线代价：**只能 0→1、不可逆**、语义 = popcount（`BOOTLOADER_APP_SEC_VER_SIZE_EFUSE_FIELD` 决定"只能升 N 次"）、且出厂需产线流程配套（串口烧写高计数值镜像会立即烧 eFuse） | MCUboot v2.4.0 `boot/zephyr/Kconfig:1120-1184` + `loader.c:615,1672-1708,2113-2141` + `image_validate.c:438-491,566-570` + `bootutil_img_security_cnt.c` + `boot/bootutil/include/bootutil/security_cnt.h`；ESP-IDF v5.5.5 `bootloader_utility.c:322-376,586-629` + `esp_efuse_fields.c:44-93` + `docs/en/api-reference/system/ota.rst:164-171,209-211` | **Q-15 首版不纳入防降级**（记演进）：direct 策略下纯版本比较不适用（生态自身即 `depends on !BOOT_DIRECT_XIP`），而硬件计数器要真正防得住必须依赖签名链——签名是 Q-04 已记的演进档；首版 CRC32 能拦随机损坏与结构错位（`image.h` 判定顺序），拦不住"结构合法、内容恶意的旧镜像"。R-IMG-2 据此收敛为**演进档需求** |
| K-35 | 后门门禁的生态形态（2026-09-16 实核）：**协议层凭证只有 OpenBLT 一家提供、且默认关闭**——XCP `GET_SEED`(0xF8) / `UNLOCK`(0xF7) + 资源位分级（CAL/PAG/DAQ/STIM/PGM），目标端**不存密钥**（核心库仅留 `XcpGetSeedHook`/`XcpVerifyKeyHook` 弱钩子，算法在主机的动态库），**无重试计数、无延时、无锁定**（解锁失败即回 `XCP_ERR_ACCESS_LOCKED` + 断开重锁）；各 demo 的 `BOOT_XCP_SEED_KEY_ENABLE` 默认 0。**其余生态全部落在协议之外**：ST ROM bootloader 无任何鉴权（门禁 = option bytes 的 RDP/WRP，RDP2 不可逆，且通常直接进不去 system memory）；mcumgr/SMP **层内无鉴权无授权**（官方 THREAT_MODEL 把"固件上传需要凭证"列为 NON-FINDING，唯一的门是 BLE 传输层属性权限，串口/UDP 无鉴权）；U-Boot fastboot **无 lock/unlock**，DFU 靠"没有 `dfu_alt_info` 就不工作"，另有编译期命令裁剪与环境变量静态 flags（`aroc`，含 write-once）。**"进入后门"与"要不要凭证"是两个独立旋钮**——OpenBLT 的后门窗口（默认 500ms 内收到任意字节即进入）不校验任何东西，凭证只作用于进入之后的资源访问 | OpenBLT v1.22.0 `Target/Include/xcp.h:88-92,219-225` + `Target/Source/xcp.c:382-403,558-620,786-810,1195-1203` + `Target/Source/backdoor.c:38-47,126-171`；ST AN3155 Rev18 Table 2 + RM0455 Rev9 §4.5.3；Mynewt `docs/THREAT_MODEL.md` §2/§9/§11a + Zephyr `subsys/mgmt/mgmt.c:36-69` + `transport/smp_bt.c`；U-Boot v2026.07 `doc/android/fastboot.rst` + `env/flags.c:37-73,451-457` | **Q-16 首版门禁 = 非凭证门槛**（进入窗口 + 触发条件 + 命令面编译期裁剪），会话凭证记演进——生态既有形态即如此，且与 R-DL-1（两槽全坏时仍能被完整灌写）**不冲突**："要不要凭证"与"能不能兜底刷写"在生态里本就是独立旋钮 |
| K-36 | 应用侧状态写入的擦除归属（2026-09-16 实核，Q-12 擦除归属的依据）：**MCUboot 的应用侧写路径完全不含擦除**——`boot_set_next()` 整棵调用树内 `flash_area_erase` **零出现**；confirm = `boot_write_image_ok()` 写 **1 字节**（`0xFF→0x01`，以 `image_ok == BOOT_FLAG_UNSET` 作"先读后写"守卫）；mark pending = 写另一槽的 `magic` + `swap_info`，同为**纯 program**。**擦除归三处**：①上传流程（imgtool `--pad` 用 `erased_val` 把整槽填 0xFF；mcumgr `img_mgmt_erase_image_data()` 先 `flash_area_flatten()`）；②bootloader 的 swap 清理（`swap_scramble_trailer_sectors()`，**倒序**擦，注释成文：不倒序则掉电后部分擦除的 scratch 会被当有效状态写回）；③direct-xip + revert 的**整槽擦**（`boot_scramble_region(fap, 0, size)`，在 bootloader 启动期）。`swap_erase_trailer_sectors()` 具"为写入而预擦"语义，但 **v2.4.0 中零调用点（dead code）**。**MCUboot 无需额外预擦机制的结构性原因**：trailer 贴在槽内 → 上传时擦槽即**顺带**预擦。上游文档对 RWW/同 bank 阻塞**完全沉默**（全仓 grep 零命中）；唯一官方讨论是 Zephyr issue #107013（H7RS/N6 + 外部 XSPI），其 workaround 之一 = "应用只写临时标志 → 复位 → bootloader 在 `boot_go_hook()` 里更新 trailer" | MCUboot v2.4.0（commit `6d3b3d2c`）`boot/bootutil/src/bootutil_public.c:376-394,527-663,689-751`（应用侧全部写入 API，全文件 `flash_area_erase` 零命中）+ `bootutil_misc.c:176-183`（"magic is always written in the last step"）+ `swap_misc.c:35-97` + `swap_scratch.c:748-915` + `loader.c:2181-2234`（direct-xip revert 整槽擦）；Zephyr `subsys/dfu/boot/mcuboot.c:461-476`；Zephyr issue #107013 | **Q-12 擦除归属改为引导侧**：写者只向"已擦副本" program，擦除由 bootloader 在引导期承担；不变式 = "池中始终至少有一份已擦副本 + 永不擦最新有效份"；**该不变式要求至少 3 份**（2 份推演见 `multi_strategy_boot_design.md` §5.1 的份数讨论：确认后池中 0 份已擦，而下一轮 PENDING 发生在任何 bootloader 运行之前）。**可移植性**：本机制只依赖 NOR 通用语义 + "引导期无应用并发"，**不依赖 RWW**（单 bank 芯片上价值更大）；RWW 只让"写 staging 镜像"一步在 F427 上免费 |
| K-37 | 各生态元数据放置与写入协议对照（2026-09-16 实核）：**ESP-IDF otadata** = 独立 data 分区、2×4KB 扇区；位置约束只有机器强制的四项——**唯一**（`gen_esp32part.py:330-338` 报 "Found multiple otadata partitions"）、**大小恰为 0x2000**（`:340-343`）、4KB 对齐、分区不重叠；**应用侧写入 = 擦 + 写**（`rewrite_ota_seq` 内先 `esp_partition_erase_range`）。**MCUboot trailer** = 槽尾单份、**纯写**（见 K-36）。**Zephyr NVS** = 独立分区、正常路径**纯写**（append 型），GC 时先搬有效条目 + 写 `gc_done` marker、**最后**才擦旧扇区，全程在**调用者线程内同步**执行。**nRF5 SDK** = flash 最后一页 + 备份页、应用侧**擦 + 写**。**Gecko BootloadInfo** = 第一个 slot 前、**两份各占一整页**（"must reside on different flash pages"）、应用侧**擦 + 写**（**先写 page1 再写 page0** 做掉电冗余）。**TI SimpleLink** = 镜像头钉在 slot 起始 + 外部 flash 元数据表；`COPY_DONE` **只由 BIM 在 boot 期写**。**三条横断结论**：①**没有一家把运行期必写状态做成纯写**，唯一例外是 MCUboot 的 `image_ok` 单字节（其擦除被挪到 bootloader）；②**没有任何框架要求"元数据与运行镜像分属不同 bank"**（多数平台无 bank 概念；ESP-IDF 的位置约束止于"独立分区 + 唯一 + 定长 + 对齐"）；③通用手段三选一 = 擦除与应用**时间错开**（MCUboot）/ 把执行代码**搬 RAM**（Gecko `MSC_RAMFUNC`、Nordic `zephyr_code_relocate`、TI "code can not execute in flash while any part is being programmed"）/ **接受并给旋钮**（ESP-IDF：`SPI_FLASH_YIELD_DURING_ERASE`、`AUTO_SUSPEND`、`WRITE_CHUNK_SIZE`） | ESP-IDF v6.1 `components/partition_table/gen_esp32part.py:325-343` + `docs/en/api-guides/partition-tables.rst:163,218-222` + `esp_ota_ops.c`；Zephyr main `subsys/kvss/nvs/` + `doc/services/storage/nvs/nvs.rst:65-69,127-134`；nRF5 SDK 17.1.0 `nrf_dfu_types.h:92-144` + `nrf_dfu_settings.c`；Gecko SDK master `btl_interface_storage.h` + UG489 v3.3.1；TI SimpleLink 5.40 `bim_offchip_main.c:551-560` + `ext_flash_layout.h:80` + driverlib `flash.h:664-667` | 元数据必须**独占可擦区域**（MCUboot `design.md:178-184` 的两条 area 属性：可独立完整擦除 + 一区写入不限制他区写入）——第二项正是同 bank NOR 无 RWW 时被打破的那条；OMR 的 meta 独立分区满足该契约，位置无需与运行镜像分 bank（无先例、也无必要） |
| K-38 | ST 侧：RWW 边界、预擦机制与耐久折算（2026-09-16 实核，Q-12 的机制与耐久依据）：**RWW 边界**——RM0090 Rev22 §3.6 原文 "Any attempt to read the flash memory on STM32F4xx while it is being written or erased, causes the bus to stall … **code or data fetches cannot be performed** while a write/erase operation is ongoing"（**同 bank 的擦/写/读全 stall**；"同 bank stall"是通用规则 + 例外范围**推得**，ST 无逐字句）；§3.6.5 给出的唯一出路 = 跨 bank 读，并明确 **"Write-while-write operations are not allowed"**（不能一边擦 A bank 一边写 B bank）且 BSY 为**全局位**（一次只允许一个擦/写操作在跑）。**RAMFUNC 纠错**——`stm32f4xx_hal_flash_ramfunc.c` 的用途是 **flash sleep / 停 flash 接口时钟**（守卫列表仅 F410/411/412/446，**F427 整份编除**），与擦/写无关；F4 HAL 从不要求把擦写代码放进 RAM（`HAL_FLASH_Program`/`HAL_FLASHEx_Erase` 均为常驻函数 + 轮询 BSY，超时 50s）。**预擦机制 = ST 官方 EEPROM 模拟**——AN3969（F40x/F41x）/AN2594（F10x）§2.1：至少两页，"**one that is initially erased, and offers byte-by-byte programmability**"、页头 16-bit 三态（`ERASED`/`RECEIVE_DATA`/`VALID_PAGE`）、需求含 "Background sector erase"、掉电靠 `EE_Init()` **每次上电立即**恢复（9 种状态组合、3 种非法）；AN4767 §6.5.2 主动把"另一个 bank 做数据 + EEPROM 模拟算法"列为双 bank 常见用法。**耐久折算（关键背书）**——AN3969 §3.4.1 / AN2594 §3.4 **逐字相同**："**A program/erase cycle consists of one or more write accesses and one page erase operation**"（同一擦除周期内多次 program **折 1 次**擦除周期）+ 寿命总量 = **页容量 × 10 000**（16KB 页 → 160MB/页，多页线性叠加）；DS9405 Rev13 Table 50：NEND ≥ **10 kcycles**、tRET 仅按 kcycle 分档（1k@85°C=30y / 10k@55°C=20y）→ **retention 与"每周期 program 次数"的关系无任何 ST 背书**。**F4 无 option-byte SWAP_BANK**，替代品是软件可写的 `SYSCFG_PMC.FB_MODE`。**附（硬件勘误）**：ES0206 2.2.15 "Data cache might be corrupted during Flash memory read-while-write operation"，workaround = 写前 `DCEN=0` → 写后 `DCRST` 复位 → 重开（Zephyr `flash_stm32f4x.c` 已实现）；OMR 现状：**ART 缓存（ICEN/DCEN/PRFTEN）全仓从未使能**（vendor `SystemInit` 为裁剪版、非 vendor 代码零使能点），故勘误当前不生效——但一旦为性能打开缓存即命中，见 `docs/internal/active/issue_000_bootloader_gap_analysis/` | RM0090 Rev22 §3.4/§3.6/§3.6.1/§3.6.2/§3.6.5/§9.3.2；DS9405 Rev13 §6.3.13 Table 48/50；AN3969 Doc ID 022108 Rev1 §2.1/§3.2/§3.3/§3.4.1；AN2594 Doc ID 13718 Rev3 §2.1/§3.4；AN4767 Rev3 §2/§3/§6.3/§6.5.2；AN2606 Rev70 §41；UM2262 Rev12 App.J；HAL `Src/stm32f4xx_hal_flash_ramfunc.c:5-9,58-59` + `Inc/stm32f4xx_hal_def.h:158-185` + `Src/stm32f4xx_hal_flash.c:94,544,557` | **Q-12 采纳"预擦 + 只 program"**：机制有 ST 官方 EEPROM 模拟的逐字对应、擦除时机有 MCUboot 先例（K-36）、耐久折算有官方定义、且与"禁止 write-while-write"天然兼容（擦除在引导期、写入在运行期，**时间错开**）。**两条必须落档的边界**：①**retention 折算无背书**——若保持性是硬指标，须按保守口径折算或自做保持试验；②**"擦除与应用不并发"须写成显式不变式**，不得设计成"bootloader 后台擦 X、应用同时写 Y"（RM0090 明文禁止 write-while-write） |

| K-39 | 跨介质槽的支持形态与几何约束（2026-09-16 实核）：**MCUboot 核心不假设同介质**——每个槽是一个 `flash_area`、各带自己的设备；Zephyr 端口有 `FLASH_PARTITION_DEV_UNIQUE(n,m)` 宏**专门判"两槽在不同器件"**，并有 `flash_area_get_device_id()`。**跨介质 swap 是一等公民**：`swap_scratch.c` 逐槽取 area、逐槽查 sector 几何，注释原文 "Since the primary and secondary slots don't necessarily have the same layout…"；2018 年 commit `2bd980a5` 专为"两槽不同扇区布局"放宽限制，**其测试即 slot0 片内 + slot1 外部 QSPI 的 4K/8K/16K 组合**。**NCS 把"secondary 放外部 flash"做成 sysbuild 标准选项**（`SB_CONFIG_PM_EXTERNAL_FLASH_MCUBOOT_SECONDARY` + DT chosen `nordic,pm-ext-flash`）+ 专章文档 + 官方测试，支持 swap-using-move / overwrite-only / direct-xip。**反面**：ESP-IDF 明文"外部 flash 上的分区不能引导"（分区表项无芯片字段）；Gecko 一个 bootloader 只有一个 storage 后端（slot 只能作下载区、**不能作启动槽**；"app 片内 + 整组 slot 片外"才是其标准形态）；TI on-chip/off-chip 二选一；OpenBLT 无槽概念。**三条几何硬约束**（`boot_slots_compatible()` 原文）：① 两槽扇区尺寸**必须互为倍数**（4K↔16K ✓ / 4K↔24K ✗）；② **scratch ≥ 两槽最大扇区**；③ swap-scratch **假设两槽面积相等**（offset = secondary 大一个扇区、move = primary 大一个扇区 + status 区），且 **offset/move 要求槽内扇区同尺寸**。**隐含前提**：外部器件须进 flash map；驱动须支持单字节读写与"片内→片外"写；`CONFIG_MULTITHREADING=y`；`BOOT_MAX_IMG_SECTORS` 按外片 4K 粒度放大（1MB → 256 扇区）；`MCUBOOT_BOOT_MAX_ALIGN` 默认取**片内** write-block-size，跨介质须显式覆盖；direct-xip 要求外片 memory-mapped 且受 RWW 阻塞（#107013）。官方文档**从未正面写"支持跨器件"**——支持性证据来自代码与生态文档，**未找到任何"不推荐"表述** | MCUboot `boot/bootutil/src/swap_scratch.c`（`boot_slots_compatible()` 注释与逐槽 sector 查询）+ `boot/zephyr/flash_map_extended.c`（`FLASH_PARTITION_DEV_UNIQUE`、`flash_area_get_device_id`）+ `docs/design.md`（swap 三节）+ commit `2bd980a5`；NCS `sysbuild/Kconfig.sysbuild` + 文档 "Using external flash memory partitions" + `tests/modules/mcuboot/external_flash`；ESP-IDF v5.5 Partitions API；Gecko `btl_storage.h` + UG489 Rev1.1；TI `bim_offchip_main.c`；OpenBLT 1.22.00 | 我们的 `devName`（分区表每行带介质）与 Zephyr `flash_area{fa_dev,fa_off,fa_size}`、NCS `pm.yml {device,region,offset,size}` **同构**——"槽可跨介质"的架构表达方向正确、有直接先例。**跨介质的成本不在"能不能"而在几何**：擦除粒度**不必一致，但必须互为倍数**、且 scratch/staging 容得下最大扇区——这两条应作为布局表的校验规则（→ K-40）。**对 F427 的推论（标注为推论）**：片内槽为变扇区（16K×4+64K+128K×7）→ **swap-offset/move 不可用**（要求槽内扇区同尺寸），只能 **swap-scratch（且 scratch ≥ 128K）** 或 overwrite/direct-xip；外片 4K 均一与片内各尺寸互为倍数 ✓。另：mcuboot issue #2120 记录 F2/F4/F7 变扇区与 Zephyr 端 `erase-block-size` 校验的冲突（closed as stale，**需按实际版本复核**） |
| K-40 | 升级几何的配置面与约束强制手段（2026-09-16 实核）：**生态分工一致**——几何数值（偏移/大小）**开放给用户**，语义约束（对齐/等大/重叠/trailer 空间/scratch 容量）由**构建期工具链**强制：imgtool（`Image size (0x..) + trailer (0x..) exceeds requested size 0x..`、`-H` 下限 32、未 `--pad-header` 时 header 必须全零）、ESP-IDF `gen_esp32part.py`（app 偏移 64KB 对齐、data 偏移 4KB、**otadata 唯一且恰 0x2000**、不重叠、不侵入表扇区、条目数上限、名字唯一）、NCS `partition_manager.py`（重叠/空洞/对齐求解，`PartitionError` + `FATAL_ERROR`）。**没有任何框架靠"框架侧几何校验"独自兜底**。**"等大性"几乎没有构建期强制**：MCUboot 只在 `design.md` 写"swap-using-scratch 假设两槽面积相等"，Zephyr 构建系统**不比较 slot size**（比的是 erase/write block size）；不等大的后果 = 运行期 `boot_slots_compatible()` 返回 0 → `loader.c` **静默跳过升级、照常启动旧镜像**（无任何用户可见报错）。同类静默项：`--max-sectors` 与 `BOOT_MAX_IMG_SECTORS` 不一致 → trailer magic 偏移错位 → **静默不升级**。**最佳实践样板 = Zephyr settings-NVS**：用户填**系数**（`SETTINGS_NVS_SECTOR_SIZE_MULT`）而非绝对值，框架用**实测擦除块大小**折算（从根上配不出未对齐值），并按分区实际大小**自动截断**扇区数。**错误表现分级**（保护强度降序）：构建期工具报错 ＞ 运行期显式错误码（NVS `-EINVAL` / Gecko `NEEDS_ALIGN`）＞ 运行期静默降级 ＞ 完全无检查（Zephyr 分区重叠、Gecko slot 重叠）。**另**：MCUboot **没有"尝试次数 N"**——回滚由 `image_ok`/`copy_done` 在下次启动决定（呼应 K-25，我们的 `OM_BOOT_ROLLBACK_LIMIT` 属不同维度） | MCUboot v2.4.0 `scripts/imgtool/{main.py,image.py}` + `docs/design.md:228-234,270,289-316,342-363,375-397,434` + `boot/zephyr/CMakeLists.txt` + `swap_scratch.c`/`bootutil_area.c`；ESP-IDF v5.3.2 `components/partition_table/gen_esp32part.py` + `check_sizes.py`；Zephyr `subsys/settings/Kconfig:120-133` + `settings_nvs.c:406-419` + `subsys/fs/nvs/nvs.c:1034-1043`；NCS `scripts/partition_manager.py`；Gecko `btl_storage_internal_flash.c:186-199`；nRF5 `nrf_dfu_types.h:94-138`；TI `bim_dual_onchip_main.c:89-122` | **我们的补强点**：布局表是**编译期常量**，故"等大性 / 对齐关系 / scratch 与 staging 的存在性与最小尺寸 / 槽容量 ≥ 镜像上限"这类**策略级约束可用 `_Static_assert` 在编译期拦下**——强于生态普遍的"运行期检查 + 静默降级"，且符合本框架"配置错误显式报错、不静默"的既有原则（partition v2 几何 fail-fast）。**工具侧**：`omimg verify` 已具 `--slot-capacity` 参数，接进构建流程即等价 imgtool 的容量校验。**可借鉴的一招**：把"用户填绝对值"改为"**填相对量 + 框架按几何折算/夹取**"（settings-NVS 范式），可整类消除配置错误 |

## 5. 更新记录（追加式，不重排编号）

- 2026-09-06：首版。素材 = 09-05 定调会话（P-01..03、五步序列）+ 09-06 成熟框架调研（K-01..13）；同日拍板 P-04、P-05（多策略设计讨论产出）。
- 2026-09-06（同日追加）：K-14/K-15（并发锁模型与设备对象判据，F-04/F-05 调研产出）；F 系列定论（F-01..05）落地为 `flash_dev_design.md` v0 定稿。
- 2026-09-06（同日追加，v1）：真机验证（rm-a/F427）驱动异步重构——同步忙等饿死低优线程实测（FreeRTOS 持续就绪压制 log LOW 线程）+ 多片并发/read 语义调研（双层锁：device 锁管本片、总线锁仅传输瞬间；read 业界保持同步）→ F-04 修订为异步写/擦 + 同步读（F-06）；K-14 含义列同步；`flash_dev_design.md` v1 定稿。
- 2026-09-06（同日追加）：K-16（完成源边界与事件范式裁决：事件不进框架 API、完成源差异留后端、bootloader 同步阻塞+拆片+主机超时、U-Boot 裁剪样板——Linux/Zephyr/CMSIS/MCUboot/OpenBLT/ST/TI/U-Boot 六方证据）；D-07 强化为"事件主路径、轮询退化"（`flash_dev_impl_design.md` §6.1）。
- 2026-09-07（追加）：K-17..20（镜像校验链实践 / 回退窗口与策略耦合 / 裸机 osal 语义参考 / 槽位映射生态——boot 设计拍板 Q-04/06/10/11 的调研产出）；K-16 含义列经修订注记指向 K-19（"裸机 osal 空桩"表述细化：空转宏为历史妥协，按面剔除 + 同步面保真为现代做法）；§6 关联文件名修正（`multi_strategy_boot_design.md`，原误记 `boot_system_multi_strategy_design.md`）。
- 2026-09-07（同日追加）：K-21（下载通道命令组架构——用户拍板：bootloader 只认镜像；通信/命令服务承载 boot 命令组；首版后门协议按命令组形态 = 服务裁剪前身；服务本体记 services 演进项与 shell 规划合并；boot 组高危需门禁——mcumgr SMP 先例）。
- 2026-09-14：K-22..K-29（镜像头契约形态 / 三个独立对齐量 / boot 状态小块三方形态与写序铁律 / 启动计数生态事实 / 下载协议帧同步与续传语义 / 跳转序列与多槽构建 / 校验算法跨平台一致性 / 启动校验策略与面积实证）——素材 = 五路源码级实核（MCUboot / ESP-IDF / nRF5 / Zephyr / mcumgr / ST AN3155 / XMODEM / OpenBLT / U-Boot / RM0090 / AN4187），逐条出处见各条目出处列。**同日 K-17、K-18 加修订注记**（三处事实更正：OpenBLT 默认校验值非 CRC32；ESP-IDF 无数值型启动计数；MCUboot direct-xip 具 revert）——动机 = 首版条目成文时素材深度不足，本次以逐字段源码实核取代二手表述；定位类结论（CRC32 起步、计数 N 次）不受影响，仅修正生态参照。新增 `boot_ota_requirements.md`（场景 → 需求 → 失败模式矩阵）。
- 2026-09-16：K-30..K-35（决策数据布局与写序 / 启动计数落盘范式 / 跳转动作清单与 ST 官方形态 / 中断责任方两案与看门狗事实 / 防降级两轴 / 后门门禁生态形态）——素材 = S2-3 主干五路实核（ESP-IDF v5.2·v5.5.5 / MCUboot v2.4.0 / U-Boot master·v2026.07 / Zephyr v4.1.0·v4.4.2 / OpenBLT v1.22.0 / Mynewt mcumgr / ST RM0090 Rev21·PM0214 Rev4·AN3155 Rev18·STM32CubeF4 IAP 例程），逐条出处与版本锚点见各条目。**两处文档纠错**：① VTOR 与 NVIC 等内核外设的权威在 **PM0214**，RM0090 全文无此内容；② ST 官方 F4 IAP 例程对应 **AN3965**（此前按 AN4657 记）。**三条关键事实经主会话独立复核**：ESP `crc` 字段注释确为 "CRC32 of ota_seq field only"；U-Boot 计数读写确以 `upgrade_available` 为唯一开关；MCUboot direct-xip 的 revert 动作为 `boot_scramble_region(fap, 0, size)`（抹掉整个槽、不写计数器）。S2-3 拍板 Q-12..Q-16 见 `multi_strategy_boot_design.md` §5、决策正文见 ADR-0025。
- 2026-09-16（当日追加）：K-36..K-38（应用侧状态写入的擦除归属 / 各生态元数据放置与写入协议对照 / ST 侧 RWW 边界·预擦机制·耐久折算）——素材 = 三路源码级与厂商文档级实核（MCUboot v2.4.0 + Zephyr main + Zephyr issue #107013；ESP-IDF v6.1 / Zephyr NVS / nRF5 SDK 17.1.0 / Gecko SDK + UG489 v3.3.1 / TI SimpleLink 5.40；ST RM0090 Rev22 / DS9405 Rev13 / AN3969 / AN2594 / AN4767 / AN2606 Rev70 / UM2262 Rev12 + 本地 HAL 源码）。**结论**：擦除归属由"写方自擦（ESP 形态）"改为"**引导侧擦、写者只 program**"（MCUboot 为唯一同构先例；ST EEPROM 模拟提供机制与耐久折算背书）；**份数待定**（不变式要求 ≥3 份，2 份推演见 §5.1）。K-30 加修订注记（份数结论被推翻、耐久折算由"无背书"改为"有背书但 retention 仍无"）。**附带发现**：ST ES0206 勘误 2.2.15（D-cache 在 RWW 期间可能损坏）——OMR 当前 ART 缓存未使能故不生效，已记入缺口清单。

- 2026-09-16（当日追加之二）：K-39..K-40（跨介质槽的支持形态与几何约束 / 升级几何的配置面与约束强制手段）——素材 = 两路实核（MCUboot main + Zephyr main + NCS v2.5.1/v3.0.2 + ESP-IDF v5.3.2/master + Gecko v4.5.1 + TI f2-sdk + OpenBLT 1.22.00；工具链侧 imgtool / `gen_esp32part.py` / `partition_manager.py`）。**要点**：跨介质槽在 MCUboot/NCS 上是官方支持形态（NCS 有 sysbuild 选项 + 专章文档 + 测试），我们分区表带 `devName` 的表达与业界同构；跨介质的成本在**几何**（扇区尺寸互为倍数、scratch ≥ 最大扇区、scratch 模式两槽等面积）而非"能不能"；**F427 变扇区片内槽推论上排除 swap-offset/move，只能 scratch（≥128K）或 overwrite/direct-xip**。配置约束方面：生态普遍"几何开放 + 构建期工具强制"，**"等大性"几乎无人做构建期强制且失效形态是静默降级**——我们的布局表是编译期常量，可用 `_Static_assert` 做得更强（已记入缺口清单 #23）。

## 6. 关联文档

- `multi_strategy_boot_design.md`（同目录）——多策略 boot 系统设计（决策定稿，Q-xx 拍板记录），约束锚点为本文件 P/K 编号
- `flash_dev_design.md`（同目录）——Flash 设备抽象 v1 定稿（F-01..F-06），开工序列步骤 ① 接口文档
- `storage_landscape.md`（同目录）——存储系统形态锚点：芯片特性谱系 → 软件义务分层推导、可擦存储族 + 随机器件族目标形态、演进留口 ER-xx
- `boot_ota_requirements.md`（同目录）——需求分析：四场景 → 需求、失败模式矩阵、非功能约束；步骤②/③ 的需求基线
